# TaskRoulette local environment — SAMPLE
# Copy this to .envrc and fill in your values.
# .envrc is gitignored — never commit real secrets.

export ANTHROPIC_API_KEY=""
export LOOPS_API_KEY=""
export GOOGLE_CLIENT_ID=""
export GOOGLE_CLIENT_SECRET=""

# Vercel deploy (only needed for manual deploys outside CI)
export VERCEL_TOKEN=""
export VERCEL_ORG_ID=""
export VERCEL_PROJECT_ID=""
